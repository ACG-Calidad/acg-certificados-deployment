# Configuración para el backend de certificados en /certificados
# Alias para hacer que /certificados apunte fuera del document root de Moodle
Alias /certificados /var/www/html/certificados

<Directory /var/www/html/certificados>
    Options -Indexes +FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
